<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Deriv Tick Data</title>

    <!-- Favicons -->
    <link rel="icon" type="image/png" href="logo.png" />
    <link rel="icon" type="image/jpeg" href="logo.jpeg" />

    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

    <style>
        /* ---------- layout & header ---------- */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #1a1a2e;
            text-align: center;
            margin: 0;
            padding: 20px;
            padding-bottom: 40px;
            color: #fff;
        }

        /* login container */
        .login-container {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .user-info {
            background: rgba(255, 255, 255, 0.1);
            padding: 8px 15px;
            border-radius: 20px;
            color: #fff;
            font-size: 14px;
            display: none;
            align-items: center;
            gap: 8px;
        }

        .user-info i { color: #16c79a; font-size: 16px; }

        .logout-btn {
            background: none;
            border: none;
            color: #ff444f;
            padding: 0 10px;
            cursor: pointer;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .logout-btn:hover { transform: translateY(-2px); color: #ff8086; }

        .login-btn {
            background: linear-gradient(135deg, #ff444f, #ff8086);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 25px;
            cursor: pointer;
            font-family: 'Poppins', sans-serif;
            font-weight: 500;
            font-size: 14px;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(255, 68, 79, 0.3);
        }

        .login-btn:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(255, 68, 79, 0.4); }

        /* modal (login only) */
        .modal {
            display: none;
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.7);
            z-index: 2000;
            backdrop-filter: blur(5px);
        }

        .modal-content {
            position: relative;
            background: rgba(26,26,46,0.95);
            width: 90%;
            max-width: 400px;
            margin: 50px auto;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            text-align: center;
            animation: modalSlide 0.3s ease-out;
        }

        @keyframes modalSlide {
            from { transform: translateY(-100px); opacity: 0; } to { transform: translateY(0); opacity: 1; }
        }

        .modal-close {
            position: absolute; top: 15px; right: 15px;
            color: #fff; font-size: 24px; cursor: pointer; width: 30px; height: 30px;
            line-height: 30px; text-align: center; border-radius: 50%; transition: all 0.3s ease;
        }
        .modal-close:hover { background: rgba(255,255,255,0.1); }

        .modal-content h2 { color: #16c79a; margin-bottom: 20px; }

        .deriv-login-btn {
            background: linear-gradient(135deg, #ff444f, #ff8086);
            color: white; border: none; padding: 15px 30px; border-radius: 25px;
            cursor: pointer; font-family: 'Poppins', sans-serif; font-weight: 500; font-size: 16px;
            transition: all 0.3s ease; box-shadow: 0 4px 15px rgba(255, 68, 79, 0.3);
            display: inline-flex; align-items: center; gap: 10px;
        }
        .deriv-login-btn img { width: 24px; height: 24px; }

        /* tables */
        h2 { color: #16c79a; margin: 30px 0 20px; font-size: 28px; font-weight: 600; text-transform: uppercase; letter-spacing: 2px; text-shadow: 0 2px 4px rgba(0,0,0,0.2); }

        .table-container { padding: 0 20px; margin: 0 auto; max-width: 1400px; }

        table {
            width: 100%; margin: 10px; border-collapse: separate; border-spacing: 0;
            background: rgba(255,255,255,0.05); backdrop-filter: blur(10px);
            border-radius: 15px; overflow: hidden; box-shadow: 0 8px 32px rgba(0,0,0,0.3);
            transition: transform 0.3s ease;
        }
        table:hover { transform: translateY(-5px); }
        th, td { padding: 15px; text-align: center; font-size: 14px; border-bottom: 1px solid rgba(255,255,255,0.1); }
        th { background-color: rgba(22,199,154,0.1); color: #16c79a; text-transform: uppercase; letter-spacing: 1.5px; font-weight: 600; font-size: 13px; }
        tr:last-child td { border-bottom: none; }
        td:first-child { color: #f0f0f0; font-weight: 500; text-align: left; }

        .signal-box {
            padding: 6px 12px; font-weight: 600; border-radius: 20px; display: inline-block; min-width: 80px;
            text-transform: uppercase; font-size: 12px; letter-spacing: 1px; transition: all 0.3s ease; border: 2px solid transparent;
        }
        .rise, .fall, .over, .under {
            background: linear-gradient(135deg, #00f2c3, #0098f0);
            color: #fff; box-shadow: 0 4px 15px rgba(0,242,195,0.3);
        }
        .neutral { background: rgba(255,255,255,0.1); color: #6c757d; border: 2px solid rgba(108,117,125,0.2); }
        .signal-box:hover { transform: scale(1.05); }

        /* responsive */
        @media (max-width: 768px) {
            thead { display: none; }
            table, tbody { display: block; width: 100%; }
            tr { display: flex; flex-wrap: wrap; margin-bottom: 20px; padding: 15px; background: rgba(255,255,255,0.05); border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.2); }
            td { display: flex; align-items: center; justify-content: space-between; width: 100%; padding: 8px; border: none; text-align: left; }
            td:first-child { width: 100%; font-size: 16px; font-weight: 600; color: #16c79a; margin-bottom: 10px; padding: 0 0 10px 0; border-bottom: 1px solid rgba(255,255,255,0.1); }
            td:not(:first-child) { width: 50%; padding: 8px 0; }
            td:not(:first-child)::before { content: attr(data-label); font-weight: 500; margin-right: 10px; color: #f0f0f0; }
            .signal-box { margin-left: auto; min-width: 70px; text-align: center; }
        }
        @media (max-width: 460px) {
            body { padding: 10px; }
            tr { margin-bottom: 15px; padding: 12px; }
            td:not(:first-child) { width: 100%; }
            .signal-box { min-width: 80px; }
        }
    </style>
</head>
<body>

    <!-- Logo Header -->
    <div style="display: flex; align-items: center; justify-content: center; margin-bottom: 20px;">
        <img src="logo.jpeg" alt="Chief Brian Logo" style="height: 80px; border-radius: 16px; box-shadow: 0 2px 12px rgba(0,0,0,0.15); margin-right: 18px; background: #fff; padding: 8px;" />
        <div style="text-align: left;">
            <span style="font-size: 2.2rem; font-weight: 700; color: #fff; letter-spacing: 2px;">CHIEF BRIAN</span><br />
            <span style="font-size: 1rem; color: #ff444f; font-weight: 500;">TRADE TO WITHDRAW</span>
        </div>
    </div>

    <!-- Login / User area (keeps login modal intact) -->
    <div class="login-container">
        <div class="user-info" id="userInfo">
            <button class="logout-btn" onclick="logout()">
                <i class="fas fa-sign-out-alt"></i>
            </button>
        </div>
        <button class="login-btn" onclick="showLoginModal()">Login</button>
    </div>

    <!-- Login Modal (unchanged) -->
    <div id="loginModal" class="modal">
        <div class="modal-content">
            <span class="modal-close" onclick="closeLoginModal()">&times;</span>
            <h2>Login to Trading Signals</h2>
            <button class="deriv-login-btn" onclick="loginWithDeriv()">
                <img src="deriv-logo.png" alt="Deriv Logo">
                Login with Deriv
            </button>
        </div>
    </div>

    <h2>Trading Signals Dashboard</h2>
    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>MARKET</th>
                    <th>RISE 📈</th>
                    <th>FALL 📉</th>
                    <th>OVER 2</th>
                    <th>UNDER 7</th>
                    <th>EVEN</th>
                    <th>ODD</th>
                    <th>DIFFER</th>
                </tr>
            </thead>
            <tbody id="signalsTable">
                <!-- signals.js should populate rows here -->
            </tbody>
        </table>
    </div>

    <!-- Safety script: ensure any paywall modal or functions are neutralized -->
    <script>
        // Remove any payment modal if present from old versions
        (function () {
            const pm = document.getElementById('paymentModal');
            if (pm && pm.parentNode) pm.parentNode.removeChild(pm);

            // neutralize common paywall functions in case they exist in signals.js
            window.showPaymentModal = window.showPaymentModal || function () { /* no-op */ };
            window.redirectToWhatsApp = window.redirectToWhatsApp || function () { /* no-op */ };

            // If signals.js checks a "isSubscribed" flag, set a permissive override.
            // This won't overwrite an explicit server check, but will help client-side gating.
            try {
                window.__FORCE_ALLOW_SIGNALS__ = true;
            } catch (e) { /* ignore */ }
        })();
    </script>

    <!-- Your existing signals loader (left in place) -->
    <script src="signals.js" type="text/javascript"></script>

    <!-- Minimal modal and UI control functions -->
    <script>
        // simple modal control for login
        function showLoginModal() {
            const m = document.getElementById('loginModal');
            if (m) m.style.display = 'block';
        }
        function closeLoginModal() {
            const m = document.getElementById('loginModal');
            if (m) m.style.display = 'none';
        }

        // placeholder login/logout functions (customize as needed)
        function loginWithDeriv() {
            // implement your Deriv OAuth / login flow here
            alert('Deriv login flow - implement your auth here.');
            closeLoginModal();
        }
        function logout() {
            // implement logout logic
            document.getElementById('userInfo').style.display = 'none';
            alert('Logged out');
        }

        // optional: if signals.js depends on DOM ready, ensure data loads after DOM is ready
        document.addEventListener('DOMContentLoaded', function() {
            // If your signals.js exposes a function to render now, you can call it here, e.g.:
            // if (typeof window.renderSignals === 'function') window.renderSignals();
            // Otherwise signals.js should run on its own.
        });
    </script>
</body>
</html>
